# Development only - uses http://localhost:8000 by default
# In production on Vercel, uses VERCEL_URL automatically (injected by Vercel)
# To override in production, set this environment variable:
VITE_API_URL=http://localhost:8000
